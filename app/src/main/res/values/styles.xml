<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="TextAppearance.TabsFont" parent="android:TextAppearance">
        <item name="android:fontFamily">@font/times_new_roman_bold</item>
        <item name="android:textSize">20dp</item>
    </style>

    <style name="TextAppearance.TabsFont2" parent="android:TextAppearance">
        <item name="android:fontFamily">@font/times_new_roman</item>
        <item name="android:textSize">20dp</item>
    </style>

    <style name="TextAppearance.TabsFont3" parent="android:TextAppearance">
        <item name="android:fontFamily">@font/times_new_roman</item>
        <item name="android:textSize">25dp</item>
    </style>

    <style name="MyTheme" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:actionButtonStyle">@style/myActionButtonStyle</item>
        <item name="actionButtonStyle">@style/myActionButtonStyle</item>
    </style>
    <style name="myActionButtonStyle" parent="Widget.AppCompat.ActionButton">
        <item name="android:width">2dp</item>
        <item name="android:height">2dp</item>
    </style>

    <style name="MyActionButtonOverflow" parent="android:style/Widget.Holo.Light.ActionButton.Overflow">
        <item name="android:src">@drawable/more72px</item>
    </style>

    <style name="SpinnerTheme" parent="android:Widget.Spinner">
        <item name="android:textSize">12sp</item>
        <item name="android:fontFamily">@font/times_new_roman</item>
    </style>

</resources>
